version: 2

project_name: f

before:
  hooks:
    - go mod tidy

builds:
  - id: f
    main: ./cmd/f
    binary: f
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

nfpms:
  - id: packages
    package_name: f
    vendor: Sergey Bychkov
    homepage: https://github.com/sergeybychkovvvpgroup-beep/f
    maintainer: Sergey Bychkov
    description: Terminal notes and command launcher with fuzzy search
    formats:
      - deb
    bindir: /usr/bin
    contents:
      - src: ./README.md
        dst: /usr/share/doc/f/README.md
